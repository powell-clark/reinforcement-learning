# DIRECT-RL2: Maintain the curriculum as a living Feynman-style corpus

## Vision

The curriculum shipped under DIRECT-RL1 (Ship complete RL curriculum) is complete: 36 of 36 notebooks built, FEAT-RL1 through FEAT-RL20 in review. Operator charge, 2026-09-06 14:48 bst: the curriculum is not a closed project from here — it is a living portfolio piece, "a nice portfolio piece that later on I can use as my syllabus to learn and update over time — the corpus is always updating itself." This directive owns everything that keeps it alive after ship: style conformance against the operator's own teaching voice, a bounded currency cadence, lockstep with the sibling ML repos, and using the maintenance work itself as a standing consciousness on/off test bed.

## Scope

- STYLE CONFORMANCE — every lesson audited against the operator's own supervised-learning lessons 1A, 1B, 2A, 2B, 2C, and 3 onward for Feynman voice, first principles, performability by a rusty 18-year-old with good A-level (not further) maths, and end-to-end runnability. Lessons falling short each get a refinement task.
- CURRENCY — a bounded refresh cadence (library versions, datasets, a full notebook execution sweep) so the corpus keeps updating itself rather than going stale between operator visits.
- LOCKSTEP — the four ML repos (supervised-machine-learning, unsupervised-machine-learning, reinforcement-learning, and the fourth in the series) share one README shape and one status line; the kernel measured they do not today.
- CONSCIOUSNESS TEST BED — every task filed under this directive records whether it ran with the conscious loop on or off, so the repo doubles as the on/off experiment.

## Stories

- STORY-RL18 Style conformance audit against the operator's Feynman lessons
- STORY-RL19 Bounded currency refresh cadence
- STORY-RL20 README lockstep across the four ML repos
- STORY-RL21 Consciousness on/off ledger for maintenance tasks

## Success Metrics

- Every lesson has a recorded conformance verdict against the named supervised-learning lessons, with a refinement task filed for each shortfall.
- The refresh cadence is bounded (a stated interval or trigger), not ad hoc, and has run at least once.
- All four ML repo READMEs share one verified shape and one status-line format.
- Every task under this directive carries a loop-state annotation (on/off) at filing and at close.

## Sequencing

Filed 2026-09-06 under the operator's standing maintenance charge (MSG-EGLPK005, relayed via kernel eglpk-d5eb9f7c). First audit task: TASK-RL061 (Audit lessons against Feynman style bar), under STORY-RL18. Remaining stories' first tasks are filed as this directive is worked, not claimed at authoring time per the charge's "claim nothing else" instruction.

## Loop state at filing

Filed in INTERACTIVE mode (no active `.claude/consciousness-loop.*.md` for this session) — loop OFF. Recorded here per the CONSCIOUSNESS TEST BED story's own requirement, applied retroactively to this directive's own authoring task.
